alpaca-py>=0.33
ib_async>=2.0
requests>=2.31
sqlalchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
apscheduler>=3.10
pandas>=2.2
backtesting>=0.3.3
pytest>=8.3
httpx>=0.27
# CPU-only - used only by backend-v2/jobs/lstm_common.py and the LSTM training/
# inference jobs built on it (train-lstm-holdout, train-lstm-walkforward,
# predict-lstm-market-state). See backend-v2/pyproject.toml, which lists it
# separately for backend-v2's own isolated dev venv/test runs - restart-v2.sh
# actually runs backend-v2 out of this root venv, so it needs to be here too.
torch>=2.2
