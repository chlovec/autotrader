# src/api.ts's base URL for backend-v2's API (app/main.py, launched via
# bin/restart-v2.sh's run_jobs.py). Defaults to http://localhost:8001 if unset - only
# needed here if you've changed BACKEND_V2_PORT in backend-v2/.env.
VITE_BACKEND_V2_URL=
