# Alpaca paper-trading credentials (from https://app.alpaca.markets/paper/dashboard/overview)
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_BASE_URL=https://paper-api.alpaca.markets

# Set to false only when deliberately trading live (Phase 8)
ALPACA_PAPER=true

# Database shared by the engine and the FastAPI backend
DATABASE_URL=sqlite:///./autotrader.db

# Risk limits enforced by engine/risk.py
MAX_POSITION_SIZE_USD=1000
MAX_DAILY_LOSS_USD=200

# Backend API
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# Email alerts (optional). Leave blank to skip email - macOS notifications always fire
# regardless. Gmail example: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_PASSWORD=an
# app password (not your regular password).
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
ALERT_EMAIL_FROM=
ALERT_EMAIL_TO=
